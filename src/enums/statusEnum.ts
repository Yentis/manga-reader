export enum Status {
  READING = 'Reading',
  COMPLETED = 'Completed',
  ON_HOLD = 'On hold',
  PLAN_TO_READ = 'Plan to read',
  DROPPED = 'Dropped'
}

export enum StatusIcon {
  'Reading' = 'import_contacts',
  'Completed' = 'done',
  'On hold' = 'pause',
  'Plan to read' = 'date_range',
  'Dropped' = 'delete'
}
