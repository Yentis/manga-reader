export enum SortType {
  TITLE = 'Title',
  SITE = 'Site',
  READ = 'Read chapter',
  CURRENT = 'Current chapter',
  DATE = 'Last updated',
  RATING = 'Rating'
}

export enum SimpleSortType {
  TITLE = 'Title',
  SITE = 'Site',
  READ = 'Progress',
  RATING = 'Rating'
}
