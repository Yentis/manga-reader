export enum RequestType {
  PLATFORM = 'platform'
}

export enum SiteRequestType {
  READ_URL = 'read_url',
  SEARCH = 'search',
  SYNC_CHAPTER = 'sync_chapter'
}

export enum KitsuRequestType {
  USER_ID = 'user_id',
  MANGA_SLUG = 'manga_slug',
  LIBRARY_INFO = 'library_info',
  LOGIN = 'login'
}

export enum RequestData {
  DATA = 'data',
  ERROR = 'error'
}
